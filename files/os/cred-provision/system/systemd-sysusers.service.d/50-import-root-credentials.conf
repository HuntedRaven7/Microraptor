# Let systemd-sysusers read the root password hash from the credential store
# (`/etc/credstore/` plaintext or `/etc/credstore.encrypted/` TPM2-sealed).
[Service]
ImportCredential=passwd.hashed-password.root
ImportCredential=passwd.plaintext-password.root
ImportCredential=passwd.shell.root